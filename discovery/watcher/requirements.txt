# Core dependencies
etcd3gw
flask
waitress

# Docker support
docker

# containerd support (requires older protobuf for compatibility)
containerd
watchdog
protobuf>=3.20,<4

# Kubernetes support
kubernetes
