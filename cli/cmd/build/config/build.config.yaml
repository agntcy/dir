# The name of the agent
name: "agent-name"

# The version of the agent
version: "v1.0.0"

# List of authors of the agent
authors:
  - "author1"
  - "author2"

# List of locators of the agent
locators:
  - type: "docker-image"
    url: "url1"
  - type: "binary"
    url: "url2"

# List of custom extensions of the agent
# Only supports extensions with the following structure:
# - name: "extension1"
#   version: "v1.0.0"
#   specs:
#     key1: "value1"
# This structure will be injected into the agent's model
extensions:
  - name: "extension1"
    version: "v1.0.0"
    specs:
      key1: "value1"
      key2: "value2"
  - name: "extension2"
    version: "v1.0.0"
    specs:
      key1: "value1"
      key2: "value2"

# Source code of the agent
source: "path/to/source"

# List of paths to ignore
source-ignore:
  - ".venv/"

# Enable or disable the LLM analyzer extension
llmanalyzer: false

# Enable or disable the CrewAI extension
crewai: false

# The skills of the agent
skills:

  # The name of the skill
  items:
    - "skill1"
    - "skill2"

# Framework related configurations
framework:

  # The type of the framework, valid values are: "crewai", "autogen", "langchain", "llamaindex"
  type: "crewai"

  # The version of the framework library
  version: "0.102.0"

# The programming language and it's version used by the agent
language:

  # The programming language of the agent
  type: "python"

  # The version of the programming language
  version: "3.12.0"
