# MkDocs project (uv)

Python dependencies for Directory docs. From repo root use `task docs:*`; or run commands here with `uv run`.

```sh
uv sync
uv run mkdocs serve
uv run mkdocs build --site-dir ../../.build/site
```
